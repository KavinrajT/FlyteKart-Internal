<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.flytekart">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:name=".Flytekart"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".ui.activity.SplashActivity"
            android:label="@string/app_name"
            android:noHistory="true"
            android:theme="@style/SplashTheme"
            android:windowSoftInputMode="adjustPan|adjustResize|stateAlwaysHidden|stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.LoginActivity"
            android:theme="@style/AppTheme.TranslucentStatusBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".ui.activity.ChangePasswordActivity"
            android:theme="@style/AppTheme.TranslucentStatusBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".ui.activity.SignUpActivity"
            android:theme="@style/AppTheme.TranslucentStatusBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".ui.activity.BaseActivity"
            android:theme="@style/ToolbarTheme" />
        <activity
            android:name=".ui.activity.HomeActivity"
            android:theme="@style/ToolbarTheme" />
        <activity
            android:name=".ui.activity.CreateOrgActivity"
            android:theme="@style/ToolbarTheme" />
        <activity
            android:name=".ui.activity.MapsActivity"
            android:label="@string/title_activity_maps"
            android:theme="@style/ToolbarTheme" />
        <activity
            android:name=".ui.activity.CreateStoreActivity"
            android:theme="@style/ToolbarTheme" />
        <activity
            android:name=".ui.activity.StoreListActivity"
            android:theme="@style/ToolbarTheme" />
        <activity
            android:name=".ui.activity.StoreActivity"
            android:theme="@style/ToolbarTheme" />
        <activity
            android:name=".ui.activity.OrdersListActivity"
            android:theme="@style/ToolbarTheme" />
        <activity
            android:name=".ui.activity.OrderDetailsActivity"
            android:theme="@style/ToolbarTheme" />
        <activity
            android:name=".ui.activity.CustomerDetailsActivity"
            android:theme="@style/ToolbarTheme" />
        <activity
            android:name=".ui.activity.ProductListActivity"
            android:theme="@style/ToolbarTheme" />
        <activity
            android:name=".ui.activity.CategoryListActivity"
            android:theme="@style/ToolbarTheme" />
        <activity
            android:name=".ui.activity.VariantListActivity"
            android:theme="@style/ToolbarTheme" />
        <activity
            android:name=".ui.activity.CreateVariantActivity"
            android:theme="@style/ToolbarTheme" />
        <activity
            android:name=".ui.activity.CreateCategoryActivity"
            android:theme="@style/ToolbarTheme" />
        <activity
            android:name=".ui.activity.SubCategoryListActivity"
            android:theme="@style/ToolbarTheme" />
        <activity
            android:name=".ui.activity.CreateSubCategoryActivity"
            android:theme="@style/ToolbarTheme" />
        <activity
            android:name=".ui.activity.CreateProductActivity"
            android:theme="@style/ToolbarTheme" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

</manifest>